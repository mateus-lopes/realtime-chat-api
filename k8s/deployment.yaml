apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
        - name: app
          image: app
          ports:
            - containerPort: 5001
          env:
            - name: MONGO_URI
              value: mongodb+srv://mateusalbano20:4y6eOaGKbYfuOC9m@cluster0.1w6ozri.mongodb.net/chat_db?retryWrites=true&w=majority&appName=Cluster0